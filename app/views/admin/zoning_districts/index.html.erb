<h1>Zoning Districts</h1>
<br />
<table class="table table-striped">
  <thead>
    <tr>
      <th>Code</th>
      <th>Name</th>
      <th>CIC Codes</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <% for zone in @zoning_districts %>
    <tr class="<%= cycle("odd", "even") -%>">
      <td><%= zone.code %></td>
      <td><%= zone.name %></td>
      <td width="100px"><%= zone.active_cic_codes.size %></td>
      <td width="80px"><%= link_to 'Permissions', admin_zoning_district_path(zone) %></td>
      <td width="50px"><%= link_to 'Edit', edit_admin_zoning_district_path(zone) %></td>
    </tr>
  <% end %>
</table>